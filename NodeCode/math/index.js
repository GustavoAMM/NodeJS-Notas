function sumar(x, y) {
  return x + y;
}
function restar(x, y) {
  return x - y;
}
function mult(x, y) {
  return x * y;
}
function dividir(x, y) {
  return x / y;
}

// module.exports = {
//   sumar,
//   restar,
//   mult,
//   dividir,
// };

export default  {
  sumar,
  restar,
  mult,
  dividir,
};

